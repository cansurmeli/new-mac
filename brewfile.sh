brew install bash							        # Bourne-Again SHell, a UNIX command interpreter
brew install bash-completion			    # Programmable completion for Bash 3.2
brew install bat							        # Clone of cat(1) with syntax highlighting and Git integration
brew install clang-format					    # Formatting tools for C, C++, Obj-C, Java, JavaScript, TypeScript
brew install cmake						      	# Cross-platform make
brew install colordiff				    		# Color-highlighted diff(1) output 
brew install ffmpeg						      	# Play, record, convert, and stream audio and video
brew install git-delta                # Syntax-highlighting pager for git and diff output
brew install git-lfs                  # Git extension for versioning large files
brew install highlight						    # Convert source code to formatted text with syntax highlighting
brew install hugo							        # Configurable static site generatoro
brew install imagemagick					    # Tools and libraries to manipulate images in many formats
brew install mas							        # Mac App Store command-line interface
brew install pandoc							      # Swiss-army knife of markup format conversion
brew install rename							      # Perl-powered file rename script with many helpful built-ins
brew install restic							      # Fast, efficient and secure backup program
brew install rsync							      # Utility that provides fast incremental file transfer
brew install speedtest-cli				    # Command-line interface for https://speedtest.net bandwidth tests
brew install tmux                     # Terminal multiplexer
brew install vim							        # Vi IMproved, a programmer's text editor
brew install wget							        # Internet file retriever
brew install youtube-dl						    # Download YouTube videos from the command-line

# POSSIBLE ADDITIONS
#brew install carthage						    # Decentralized dependency manager for Cocoa
#brew install ghc							        # Glorious Glasgow Haskell Compilation System
#brew install octave						      # High-level interpreted language for numerical computing
#brew install python						      # Interpreted, interactive, object-oriented programming language
